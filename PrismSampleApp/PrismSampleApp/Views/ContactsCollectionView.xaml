<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com" 
             xmlns:ViewModel="clr-namespace:PrismSampleApp.ViewModels"
             x:Class="PrismSampleApp.Views.ContactsCollectionView"
             NavigationPage.HasNavigationBar="False"
             x:Name="contactsCollection">
    
    <CollectionView ItemsSource="{Binding BindingContext.CollectionList, Source={x:Reference contactsCollection}}" BackgroundColor="Red">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                    <Label Text="{Binding ContactName}" TextColor="Black" BackgroundColor="Magenta" FontSize="large"
                                   VerticalTextAlignment="center"/>
                    <Label Text="{Binding ContactNum}" TextColor="Black" BackgroundColor="Moccasin" FontSize="large"
                                   VerticalTextAlignment="center"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>    
</ContentPage>