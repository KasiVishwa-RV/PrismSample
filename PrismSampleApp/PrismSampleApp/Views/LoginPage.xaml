<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             xmlns:ViewModel="clr-namespace:PrismSampleApp.ViewModels" 
             x:DataType="ViewModel:LoginPageViewModel"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="PrismSampleApp.Views.LoginPage"
             Title="{Binding Title}"
             BackgroundImageSource="background.png">

    
    <StackLayout VerticalOptions="Center">
        <Frame HeightRequest="40" WidthRequest="30" BorderColor="Black" BackgroundColor="#00FFFFFF">
        <Entry PlaceholderColor="LightPink" TextColor="HotPink" Text="{Binding Username}" VerticalOptions="CenterAndExpand" Placeholder="Name"/>
        </Frame>
        <Frame HeightRequest="40" WidthRequest="30" BorderColor="Black" BackgroundColor="#00FFFFFF">
        <Entry Text="{Binding Password}" PlaceholderColor="Gold" TextColor="DarkGoldenrod" VerticalOptions="CenterAndExpand" Placeholder="Password" IsPassword="True"/>
        </Frame>
        <Button Text="Sign in" VerticalOptions="Center" Command="{Binding LoginCommand}" CommandParameter="/NavigationPage/MainPage"/>
    </StackLayout>
</ContentPage>