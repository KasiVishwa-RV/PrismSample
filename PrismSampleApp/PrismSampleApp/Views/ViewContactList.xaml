<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="PrismSampleApp.Views.ViewContactList">
    <StackLayout>
        <CollectionView Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Grid.Row="3"
                        ItemsSource="{Binding ContactList}">

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <StackLayout>
                    <Label  Text="{Binding Name}" TextColor="black" BackgroundColor="aquamarine" FontSize="large"
                                   VerticalTextAlignment="center"/>
                        <Label  Text="{Binding Number}" TextColor="White" BackgroundColor="aquamarine" FontSize="large"
                                   VerticalTextAlignment="center"/>
                    </StackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>

    </CollectionView>
    </StackLayout>
</ContentPage>